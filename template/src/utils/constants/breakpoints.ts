export type BreakpointMap = { [key: string]: boolean };

const breakpoints: { [key: string]: number } = {
  xs: 480,
  sm: 576,
  md: 768,
  lg: 992,
  xl: 1200,
  xxl: 1600,
};

// export type Breakpoints breakpoints;

export default breakpoints;
