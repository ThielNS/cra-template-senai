/**
 *
 * {{ pascalCase name }} Hook
 *
 */

import { useState } from 'react';
import { Use{{pascalCase name}} } from './types';

function use{{pascalCase name}}(): Use{{pascalCase name}} {
  const [data, setData] = useState();

  return [data, setData];
}

export default use{{pascalCase name}};
